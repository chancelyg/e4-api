# e4-api 环境变量配置示例
# 复制此文件为 .env 并填写实际值
# 优先级：显式环境变量 > .env > config.yaml > 内置默认值
# 建议：部署地址与敏感密钥放 .env，稳定业务配置放 config.yaml

# 服务监听配置（部署相关，建议放 .env）
E4_SERVER_HOST=127.0.0.1
E4_SERVER_PORT=8080
E4_SERVER_MODE=release

# 会话签名密钥（敏感，生产环境必须修改）
E4_AUTH_SECRET=replace-with-long-random-secret
